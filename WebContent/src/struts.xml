<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
        "http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>
    <!-- devMode equals debug information and reload everything for every request -->
    <constant name="struts.devMode" value="true" />
    <constant name="struts.ui.theme" value="simple" />

    <package name="ServerClient" extends="struts-default">

    <default-action-ref name="index" />

    <global-results>
        <result name="error">/error.jsp</result>
    </global-results>

    <!-- all exceptions not caught by the application will lead to error.jsp -->
    <global-exception-mappings>
        <exception-mapping exception="java.lang.Exception" result="error" />
    </global-exception-mappings>

    <!-- 'index' action leads to the view provided by index.jsp -->
    <action name="index">
        <result>/index.jsp</result>
    </action>

    <!-- 'login' action calls 'execute' in 'LoginAction' -->
    <action name="register" class="Server" method="register">
        <result name="success">register.jsp</result>
    </action>

    <action name="menuPrincipal" class="Server" method="menuPrincipal">
        <result name="success">menuPrincipal.jsp</result>
    </action>

    <action name="critic" class="Server" method="critic">
        <result name="success">critic.jsp</result>
    </action>

    <action name="insertCritic" class="action.Critic" method="execute">
        <result name="success">critic.jsp</result>
        <result name="error">critic.jsp</result>
    </action>

    </package>
</struts>